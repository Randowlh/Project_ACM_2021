A{
    while(1)
        读出
    if(偶数)
        p(empty)
        p(mutex)
        放入
        v(mutexji)
        v(fullou)
    else 
       p(empty)
        p(mutex)
        放入
        v(mutexji)
        v(fullji) 
}
B{
    while(1) 
        p(fullji)
        p(mutex)
}